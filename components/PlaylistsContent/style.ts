import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    wrapper: {
        alignItems: "center",
        width: 300,
        height: 100,
        marginInline: "auto",
        marginBlock: "auto",
    },
    input: {
        backgroundColor: "white",
        width: 200,
        height: 40,
    },
});
