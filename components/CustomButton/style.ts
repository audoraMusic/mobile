import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        borderWidth: 1,
        width: 100,
        marginInline: "auto",
        marginBlock: 30,
        borderRadius: 5
    },
    text: {
        paddingBlock: 20,
        marginInline: "auto"
    }
})